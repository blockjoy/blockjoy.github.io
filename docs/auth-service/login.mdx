---
sidebar_position: 3
---

import { VersionType } from '@site/src/components/VersionType/VersionType.tsx';
import { ApiMethod } from '@site/src/components/ApiMethod/ApiMethod.tsx';

# Login

<ApiMethod method="rpc" name="Login" />

Login user, i.e. retrieve token

<ApiMethod method="requests" name="AuthServiceLoginRequest" />

<VersionType>blockjoy.v1.AuthServiceLoginRequest</VersionType>

| Field    | Type   | Description |
| -------- | ------ | ----------- |
| email    | string |             |
| password | string |             |

<ApiMethod method="returns" name="AuthServiceLoginResponse" />

<VersionType>blockjoy.v1.AuthServiceLoginResponse</VersionType>

| Field   | Type   | Description                                                                                                                                                                                                                                                                                              |
| ------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| rerfesh | string | The refresh token is used to obtain a new token without having to provide the email and password again. Note that if you are a browser client, this field can be ignored, because we also include a HTTP-Only, Set-Cookie header. This means that this token does not have to be stored in localStorage. |
| token   | string | This is the auth token that you will include with each request. It is a Json Web Token, and therefore it may be decoded and inspected by the client to seen the allowed permission                                                                                                                       |
