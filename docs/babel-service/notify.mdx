---
sidebar_position: 4
---

import { VersionType } from '@site/src/components/VersionType/VersionType.tsx';
import { ApiMethod } from '@site/src/components/ApiMethod/ApiMethod.tsx';
import { TableCellWithOption } from '@site/src/components/TableCellWithOption/TableCellWithOption.tsx';
import { Table } from '@site/src/components/Table/Table.tsx';
import { TableHead } from '@site/src/components/Table/TableHead.tsx';
import { TableBody } from '@site/src/components/Table/TableBody.tsx';
import { TableBodyRow } from '@site/src/components/Table/TableBodyRow.tsx';
import { TableBodyRowWithDropdown } from '@site/src/components/Table/TableBodyRowWithDropdown.tsx';

# Notify

<ApiMethod method="rpc" name="Notify" />

Notify about a new version of node image

<ApiMethod method="requests" name="BabelServiceNotifyRequest" />

<VersionType>blockjoy.v1.BabelServiceNotifyRequest</VersionType>

Message to send when a new version of node image is released

| Field  | Type        | Description                                                                                                                                                  |
| ------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| config | BabelConfig | <p><strong>Field:</strong> Configuration of the node</p><p><strong>BabelConfig:</strong> Message to send when a new configuration in cookbook is created</p> |
| uid    | string      | UUID of the version request                                                                                                                                  |

<Table>
    <TableHead items={['Field', 'Type', 'Description']} />
    <TableBody>
        <TableBodyRowWithDropdown
         rows={[
                {
                    cells: [
                        {
                            name: 'config',
                            isDropdown: true,
                        },
                        { name: 'BabelConfig' },
                        {
                            name: (
                                <>
                                    <p>
                                        <strong>Field:</strong> Configuration of
                                        the node
                                    </p>
                                    <p>
                                        <strong>BabelConfig:</strong> Message to
                                        send when a new configuration in
                                        cookbook is created
                                    </p>
                                </> 
                            ),
                        },
                    ],
                    newRow: [
                        {
                            cells: [
                                {name: 'node_type'},
                                { name: 'string' },
                                { name:'snake_cased name of the node type'},
                            ],
                        },
                        {
                            cells: [
                                {name: 'node_version'},
                                { name: 'string' },
                                { name:'semantic version string of the node type version'},
                            ],
                        },
                        {
                            cells: [
                                {name: 'protocol'},
                                { name: 'string' },
                                { name:'snake_cased name of the blockchain'},
                            ],
                        },
                    ],
                },
            ]}
             />
        <TableBodyRow
            items={['uuid', 'string', 'UUID of the version request']}
        />
    </TableBody>

</Table>

<ApiMethod method="returns" name="BabelServiceNotifyResponse" />

<VersionType>blockjoy.v1.BabelServiceNotifyResponse</VersionType>

| Field    | Type                                                 | Description |
| -------- | ---------------------------------------------------- | ----------- |
| node_ids | <TableCellWithOption text="string" info="repeated"/> |             |

<TableWithDropdown />
