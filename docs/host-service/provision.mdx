---
sidebar_position: 6
---

import { VersionType } from '@site/src/components/VersionType/VersionType.tsx';
import { ApiMethod } from '@site/src/components/ApiMethod/ApiMethod.tsx';
import { CustomCodeBlock } from '@site/src/components/CustomCodeBlock/CustomCodeBlock.tsx';
import { codes } from './codeblocks/provision/codeblocks';
import { TableCellWithOption } from '@site/src/components/TableCellWithOption/TableCellWithOption.tsx';
import { Table } from '@site/src/components/Table/Table.tsx';
import { TableHead } from '@site/src/components/Table/TableHead.tsx';
import { TableBody } from '@site/src/components/Table/TableBody.tsx';
import { TableBodyRow } from '@site/src/components/Table/TableBodyRow.tsx';

# Provision

This endpoint creates a new host from an already created host provision.

<ApiMethod method="rpc" name="Provision" />

<ApiMethod method="requests" name="HostServiceProvisionRequest" />

<VersionType>blockjoy.v1.HostServiceProvisionRequest</VersionType>

<Table>
  <TableHead items={['Field', 'Type', 'Description']} />
  <TableBody>
    <TableBodyRow items={['cpu_count', 'unsigned int (64 bit)', '']} />
    <TableBodyRow
      items={[
        'disk_size_bytes',
        'unsigned int (64 bit)',
        'The amount of disk space in bytes that the host has.',
      ]}
    />
    <TableBodyRow items={['ip', 'string', '']} />
    <TableBodyRow
      items={[
        'mem_size_bytes',
        'unsigned int (64 bit)',
        'The amount of memory in bytes that the host has.',
      ]}
    />
    <TableBodyRow items={['name', 'string', '']} />
    <TableBodyRow items={['os', 'string', '']} />
    <TableBodyRow items={['os_version', 'string', '']} />
    <TableBodyRow items={['provision_token', 'string', '']} />
    <TableBodyRow items={['status', 'HostConnectionStatus', '']}>
      <TableBodyRow
        items={['HOST_CONNECTION_STATUS_UNSPECIFIED', '= 0', '']}
        rowCounter={1}
      />
      <TableBodyRow
        items={['HOST_CONNECTION_STATUS_ONLINE', '= 1', '']}
        rowCounter={1}
      />
      <TableBodyRow
        items={['HOST_CONNECTION_STATUS_OFFLINE', '= 2', '']}
        rowCounter={1}
      />
    </TableBodyRow>
    <TableBodyRow items={['version', 'string', '']} />
  </TableBody>
</Table>

<CustomCodeBlock codes={codes} />

<ApiMethod method="returns" name="HostServiceProvisionResponse" />

<VersionType>blockjoy.v1.HostServiceProvisionResponse</VersionType>

| Field   | Type   | Description |
| ------- | ------ | ----------- |
| host_id | string |             |
| refresh | string |             |
| token   | string |             |
