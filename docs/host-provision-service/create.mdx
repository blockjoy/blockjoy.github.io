---
sidebar_position: 2
---

import { VersionType } from '@site/src/components/VersionType/VersionType.tsx';
import { ApiMethod } from '@site/src/components/ApiMethod/ApiMethod.tsx';
import { CustomCodeBlock } from '@site/src/components/CustomCodeBlock/CustomCodeBlock.tsx';
import { codes } from './codeblocks/create/codeblocks';
import { TableCellWithOption } from '@site/src/components/TableCellWithOption/TableCellWithOption.tsx';
import { Table } from '@site/src/components/Table/Table.tsx';
import { TableHead } from '@site/src/components/Table/TableHead.tsx';
import { TableBody } from '@site/src/components/Table/TableBody.tsx';
import { TableBodyRow } from '@site/src/components/Table/TableBodyRow.tsx';

# Create

<ApiMethod method="rpc" name="Create" />

Create a single host provision

<ApiMethod method="requests" name="HostProvisionServiceCreateRequest" />

<VersionType>blockjoy.v1.HostProvisionServiceCreateRequest</VersionType>

| Field                                                    | Type   | Description |
| -------------------------------------------------------- | ------ | ----------- |
| ip_gateway                                               | string |             |
| ip_range_from                                            | string |             |
| ip_range_to                                              | string |             |
| <TableCellWithOption text="org_id" warning="optional" /> | string |             |

<CustomCodeBlock codes={codes} />

<ApiMethod method="returns" name="DiscoveryServiceServicesResponse" />
<VersionType>blockjoy.v1.DiscoveryServiceServicesResponse</VersionType>

<Table>
  <TableHead items={['Field', 'Type', 'Description']} />
  <TableBody>
    <TableBodyRow
      items={['host_provision', 'HostProvision', 'HostProvision entity']}
    >
      <TableBodyRow
        items={[
          <TableCellWithOption text="claimed_at" warning="optional" />,
          'Timestamp',
          'If this HostProvision has been used to create a Host, the moment of host creation will be placed here.',
        ]}
        rowCounter={1}
      >
        <TableBodyRow items={['nanos', 'int (32 bit)', '']} rowCounter={2} />
        <TableBodyRow items={['seconds', 'int (64 bit)', '']} rowCounter={2} />
      </TableBodyRow>
      <TableBodyRow items={['created_at', 'Timestamp', '']} rowCounter={1}>
        <TableBodyRow items={['nanos', 'int (32 bit)', '']} rowCounter={2} />
        <TableBodyRow items={['seconds', 'int (64 bit)', '']} rowCounter={2} />
      </TableBodyRow>
      <TableBodyRow
        items={[
          <TableCellWithOption text="host_id" warning="optional" />,
          'string',
          'This field is populated only if this HostProvision has been used to create a host. After that happens, the created host’s id will be placed here.',
        ]}
        rowCounter={1}
      />
      <TableBodyRow items={['id', 'string', '']} rowCounter={1} />
      <TableBodyRow
        items={[
          <TableCellWithOption text="install_cmd" warning="optional" />,
          'string',
          '',
        ]}
        rowCounter={1}
      />
      <TableBodyRow items={['ip_gateway', 'string', '']} rowCounter={1} />
      <TableBodyRow items={['ip_range_from', 'string', '']} rowCounter={1} />
      <TableBodyRow items={['ip_range_to', 'string', '']} rowCounter={1} />
      <TableBodyRow
        items={[
          <TableCellWithOption text="org_id" warning="optional" />,
          'string',
          '',
        ]}
        rowCounter={1}
      />
    </TableBodyRow>
  </TableBody>
</Table>
